{
  "extends": "../prefab-shared-styles-module/tsconfig.json",
  "compilerOptions": {
    // Optional: Add specific overrides for this microfrontend if needed
    "baseUrl": "./src",
    "paths": {
      "@/*": ["./src/*"],  // General alias for the 'src' folder
      "ui": ["../prefab-shared-styles-module/src/components/ui"],  // Adjust to match the correct path for 'ui'
      "components": ["../prefab-shared-styles-module/src/components"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "src/components/ui/*.tsx"
  ]
}